#include <cstdio>
#include <fstream>
#include <stdio.h>


// Бисер
int main(int argc, char** argv)
{
    long long N;
    scanf_s("%lld", &N);
    long long answer = N + 1;
    printf("%lld", answer);
    return 0;
}